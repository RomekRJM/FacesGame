<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rjm.romek.facegame"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="preferExternal"
    >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity android:name="rjm.romek.facegame.ui.activity.LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="rjm.romek.facegame.ui.activity.Game">
        </activity>

        <activity android:name="rjm.romek.facegame.ui.activity.MainMenu">
        </activity>

        <activity android:name="rjm.romek.facegame.ui.activity.EndGame">
        </activity>

    </application>

</manifest>
